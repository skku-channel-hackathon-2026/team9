pnpm-lock.yaml
**/dist/
**/node_modules/
server/src/config.ts
server/src/function-endpoint.ts
server/src/function-endpoint.test.ts

# Organiser-maintained guide. Prettier 3.9.6 measures East Asian column
# widths differently from the version it was written with, so checking it
# fails on a table it did not change. Left out rather than reformatting a
# file the organisers own.
HACKATHON.ko.md
